# 更新日志（Changelog）

本文记录 **文档转 Markdown（doc2md）** 各版本「更新了什么、解决了什么」。  
下载最新版：https://github.com/appuser-beep/doc2md/releases/latest

---

## v1.7.11（当前）

**更新了什么**
- 表格清理逻辑：短分隔行补齐、轻量模式在表头/分隔列数不一致时重写
- Excel 宽合并折叠规则收紧
- Markdown 图片 `alt` 含 `]` 时的修复
- 相关回归测试

**解决了什么**
- 短分隔行被空串补齐后变成「假数据行」，表格结构被破坏
- 问卷类「是|是|是」等值多列被误压成一格，大模型读表语义错误
- 图片文件名含 `]` 时 Markdown 图链被截断

---

## v1.7.10

**更新了什么**
- 大模型 / Azure / 高级设置的中文释义与提示
- 「格式说明」名词解释、设置指南、常见问题
- README / 使用说明中关于模型选择的说明

**解决了什么**
- 用户误以为「任意模型名都能用」；现已写明须 **OpenAI 兼容 + 支持识图（vision）**（如 gpt-4o）
- 设置项英文（Endpoint、Base URL、llm_model 等）缺少白话解释
- Azure 与大模型两套账号容易混淆

---

## v1.7.9

**更新了什么**
- 仓库地址、版本号与发布脚本对齐到 `appuser-beep/doc2md`
- ZIP 不安全路径（`../`、盘符路径）跳过
- 打包时 OCR 组件缺失则失败（避免「启用 OCR」空转）
- 发布脚本禁止强制改写已有 Release 标签
- Azure 设置窗口可滚动、可调整大小

**解决了什么**
- 文档写错 GitHub 地址 / 版本横幅过旧，用户下错地方
- ZIP 路径穿越条目仍写入 Markdown 标题
- OCR 未打进包却仍显示可启用
- 小屏笔记本上看不到 Azure 设置的保存按钮

---

## v1.7.8

**更新了什么**
- ZIP 中文文件名解码（Windows/GBK 常见乱码）
- Excel 成对重复列折叠（合并单元格填充导致的双列表头）

**解决了什么**
- `档案整理活动.zip` 等包内中文文件名显示为乱码，大模型无法对应「哪个附件」
- `国奖分享会在线表格.xlsx` 出现 `| 年级专业 | 年级专业 | 学号 | 学号 |`，列语义翻倍

---

## v1.7.7

**更新了什么**
- 表宽推断：以表头/分隔行为准，避免过宽数据行抬高列数
- 轻量清理保留 GFM 对齐（`:---:` 等）；完好表可不重写
- Notebook 输出：无 HTML 时保留 `text/markdown`，可与图片并存
- 设置界面中文标签 +「格式说明」分页签（支持格式 / 名词解释 / 设置指南 / 常见问题）
- 本机真实业务文档抽检脚本

**解决了什么**
- 短分隔行把真列合并掉
- 轻量清理抹掉表格对齐
- Notebook 有图时丢掉 markdown 说明
- 设置里英文项看不懂

---

## v1.7.6

**更新了什么**
- 表格中间列未转义 `|` 的智能合并
- Excel 区位货币（如 `[$¥-804]`）、科学计数、千分位小数位
- CLI 转换后调用扫描 PDF / 空结果后检提示
- ZIP 跳过 `.min.js` 等复合后缀

**解决了什么**
- `张|三` 一类中间竖线被并错列
- 科学计数被格式化成普通整数、小数位被 `.00` 子串误判
- CLI 对几乎无文字的扫描 PDF 缺少提示
- ZIP 内压缩 JS 仍被当源码展开

---

## v1.7.5

**更新了什么**
- 清理表格时跳过 fenced code 内的伪表
- 单元格 `|` 转义幂等（不重复加反斜杠）
- 货币符号保留

**解决了什么**
- 代码块里的示例表格被误改、误转义
- 已转义的 `\|` 再次清理变成 `\\|`

---

## v1.7.4

**更新了什么**
- Word 空表头提升为真表头
- 单元格内竖线转义
- Excel 日期 / 百分比 / 货币可读输出

**解决了什么**
- Word 表第一行空白导致 Markdown 表头无意义
- 单元格内 `|` 拆坏列
- 日期显示成序列号、比例显示成 0.15

---

## v1.7.3

**更新了什么**
- Excel 无缓存计算结果时保留公式文本
- CLI 预检（伪 RAR、空文件等硬阻断）
- `keep_data_uris` 在增强路径生效
- 排班类门禁样例与插件在冻结 exe 中的列表修复

**解决了什么**
- 公式格转换后变空
- 假 ZIP（实为 RAR）转换失败信息不清晰
- 打包后 `--list-plugins` 看不到 OCR 等插件

---

## v1.7.2

**更新了什么**
- 可选保留内嵌图（base64）
- 插件列表 / 自定义插件脚本
- 正式化格式说明文案
- 首个面向用户的发布与文档体系

**解决了什么**
- 内嵌图一律被截断、无法按需保留
- 缺少对外使用说明与发布流程

---

## v1.7.0 / v1.6.x

**更新了什么**
- 高级设置、命令行 `doc2md-cli`、Docker
- Azure / 大模型 GUI
- Office 本地增强与一批回归测试

**解决了什么**
- 仅有图形流程、不便批量与自动化
- 扫描件 / 图片描述缺少可配置云能力入口

---

## 维护约定

发新版本时请同步更新：

1. `converter.py` → `APP_VERSION`
2. 本文件 `CHANGELOG.md`（写清「更新了什么 / 解决了什么」）
3. `README.md` 版本表与当前版本横幅
4. `docs/使用说明.md` 版本号与版本摘要
5. `scripts/publish.ps1` 中的 `$version`
6. GitHub Release 说明（与本文件对应条目一致）
