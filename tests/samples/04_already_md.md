# 已有 Markdown 样例

这是一份 **已带结构** 的 Markdown，用于验证“Markdown → Markdown”是否保持可读。

## 要点

1. 标题层级应保留
2. 列表与表格应保留
3. 代码块应保留

| 指标 | 值 |
|------|----|
| 准确率 | 96% |
| 召回率 | 91% |

```python
print("hello llm")
```

> 引用：LLM 更关心语义结构，而非字号颜色。
